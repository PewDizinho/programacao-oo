/**
 * Interface que define o contrato para objetos que podem ser impressos.
 * Qualquer classe que implementa esta interface deve fornecer uma maneira de exibir seus dados.
 */
interface Imprimivel {
    /**
     * Exibe os dados do objeto de uma forma específica para cada tipo.
     */
    void imprimir();
}
